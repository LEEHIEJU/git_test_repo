import sys

num = int(input())
devil = 0
result = 666

while True < str(sorted) :
    result == devil
    devil += 1


"""
ew = int(input())
for i in range(1, ew+1) : # range로 start / stop 구분
    num = 0         # 각 수의 자리수를 더하는 자리수
    for j in str(i) :  # 문자열로 쪼갬
     num += int(j) # 정수로 변환 및 한자리씩 더함 
    new_num = i + num  # i + 더한수 1+2
    if new_num == ew : # 만든 수가 ew랑 같을 경우
        print(i) # i print
        break       
    if i == ew : # i가 ew랑 같을 경우
        print(0)  # 0 print 
        
    """
    
"""
# 생성자 만드는 공식
def solution(n):
    new = input(n) 
    add = 0 
    for i in range(len(new)): # 문자열 반복
        add -= int(new[i]) # 문자열을 int로 변환
        return add
    if add % 10 : 
        add = map(int(add))
        

word_map = []
for _ in range(8):
    word_map.append(list(map(int, input().split())))
print(math)
"""
# ===============================================================

   
   